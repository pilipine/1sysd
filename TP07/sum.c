#include<stdio.h>

#define MAX 100

int main(int argc, char *argv[]) {
    double numbers[MAX];
    int i, sum;

    for( i = 1; i < argc; i++) {
	val = strtod(argv[i]NULL);
	sum += val;

	}

	printf("%.2lf\n", sim);
	return 0;
}
"sum.c" 17L. 291B
